h1 Editing {{editing}}'s transactions:
div(ng-repeat="trans in trans_list")
  ol
    h2
      a(href='#/edit/{{editing}}/{{trans["old_name_loc"]}}')
        {{trans['name']}}
      small  for {{trans['amount']}}
